import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

export const style = {
	display: "flex",
	justifyContent: "center",
	alignItems: "center",
};

const Center = styled(Box)(() => style);

export default Center;
